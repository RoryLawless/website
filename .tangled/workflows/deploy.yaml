when:
  - event: ["push"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs/nixpkgs-unstable:
    - bun
    - nodejs

steps:
  - name: install
    command: "bun ci"
  - name: deploy
    command: "bun run wrangler deploy"
