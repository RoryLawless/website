when:
  - event: ["push", "manual"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs/nixpkgs-unstable:
    - bun

steps:
  - name: deploy
    command: "bunx wrangler deploy --config ./wrangler.jsonc"
