when:
  - event: ["push"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs/nixpkgs-unstable:
    - bun
    - nodejs

steps:
  - name: deploy
    command: "bunx wrangler deploy"
    environment:
      TERM: "dumb"
      NO_COLOR: "true"
      WRANGLER_LOG: "debug"
