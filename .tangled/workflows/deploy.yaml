when:
  - event: ["push", "manual"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs/nixpkgs-unstable:
    - bun

steps:
  - name: install wrangler
    command: "bun install"
  - name: deploy
    command: "bun wrangler deploy"
